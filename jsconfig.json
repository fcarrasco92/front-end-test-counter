{
    "compilerOptions": {
      "baseUrl": "src",
      "paths": {
        "components*": ["components*"],        
        "config*": ["config*"]
      }
    },
    "exclude": ["node_modules", "dist"]
  }
  